import chai from "chai";
import chunk from "../src/chunk.js";

const expect = chai.expect

describe('Chunk', () => {
		//it("['a', 'b', 'c', 'd'] should become ['a', 'b'], ['c', 'd']", () => {
		//	expect(chunk(['a', 'b', 'c', 'd'], 2)).to.deep.equal([['a', 'b'], ['c', 'd']])
		//})
		//it("['a', 'b', 'c', 'd'] should become ['a', 'b', 'c'], ['d']", () => {
		//	expect(chunk(['a', 'b', 'c', 'd'], 3)).to.deep.equal([['a', 'b', 'c'], ['d']])
		//})
});